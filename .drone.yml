workspace:
  path: src

pipeline:
  build:
    image: webhippie/hugo:latest
    commands:
      - hugo

  upload:
    image: plugins/s3-sync
    acl: public-read
    region: us-east-1
    bucket: readme.drone.io
    source: public/
    target: /
    delete: true
    content_type:
      ".svg": image/svg+xml
    when:
      event: push
      branch: master
